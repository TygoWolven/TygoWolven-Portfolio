---
// Import Global CSS File
import "../assets/styles/global.css";

// Import Layout Components
import Footer from "../components/organisms/Footer.astro";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, viewport-fit=cover" />
		<link rel="icon" href="/images/Website-Icon.png" />

		<link rel="preload" href="../assets/scripts/preloader.js" as="script">
		<link rel="preload" href="/fonts/Poppins-Regular.ttf" as="font" type="font/ttf">
		
		<meta name="generator" content={Astro.generator} />
		<title>Tygo Wolven | Creative Front-end Designer & Developer</title>
	</head>
	<body>
		<script src="../assets/scripts/preloader.js"></script>

		<slot />
		<Footer />

		<script src="../assets/scripts/script.js"></script>
	</body>
</html>

<style>
	/* Automatic view-transitions */
	@view-transition {
		navigation: auto;
	}

	/* Animation applied to the old page */
	::view-transition-old(root) {
	  	animation: 0.5s ease-in both outgoingAnimation;
	}

	/* Animation applied to the new page */
	::view-transition-new(root) {
	  	animation: 0.5s ease-in both incomingAnimation;
	}

	@keyframes outgoingAnimation {
		from {
			transform: translateX(0);
		}

		to {
			transform: translateX(-100%);
		}
	}

	@keyframes incomingAnimation {
		from {
			transform: translateX(100%);
		}

		to {
			transform: translateX(0);
		}
	}
</style>