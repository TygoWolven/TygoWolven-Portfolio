<div class="preloader">
    <span class="small-title content">
        Loading portfolio
        <div class="loading-dot">.</div>
        <div class="loading-dot">.</div>
        <div class="loading-dot">.</div>
    </span>
</div>

<style>
    div.preloader {
        display: none;
        position: fixed;
        align-items: center;
        pointer-events: none;
        flex-direction: column;
        justify-content: center;

        z-index: 1;
        width: 100%;
        height: 100%;
        background: var(--black);

        span.content {
            display: flex;
            color: var(--white);

            div.loading-dot {
                margin-right: 4px;
                color: var(--white);
                animation: loadingDot 1s ease infinite;

                &:nth-of-type(2) {
                    animation-delay: 0.3s;
                }
                &:nth-of-type(3) {
                    animation-delay: 0.6s;
                }
            }
        }
    }

    @keyframes loadingDot {
        0%, 33%, 66%, 100% {
            opacity: 0;
        }
        33%, 66% {
            opacity: 1;
        }
    }
</style>
